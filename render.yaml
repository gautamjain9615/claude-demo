services:
  - type: web
    name: bookstore-api
    runtime: go
    buildCommand: go build -o bin/bookstore ./cmd/server
    startCommand: ./bin/bookstore
    envVars:
      - key: PORT
        value: "8080"
    autoDeploy: true
